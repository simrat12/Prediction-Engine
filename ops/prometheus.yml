global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "prediction-engine"
    metrics_path: /metrics
    fallback_scrape_protocol: PrometheusText0.0.4
    static_configs:
      - targets: ["engine:9000"]
